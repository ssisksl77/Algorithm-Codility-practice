package linkedlist;

import java.util.LinkedList;

class Result {
/*	public Result(Node object, boolean b) {
		this.node = object;
		this.result = b;
	}
	public Node node;
	public boolean result;
}
public class ex02 {
Result isPal(Node head, int length){
	if(head == null || length ==0){
		return new Result(null, true);
	}else if(length == 1){
		return new Result(head.next, true);
	}else if(length ==2 ){
		return new Result(head.next.next,
				head.data == head.next.data);
	}
	Result res = isPal(head.next, length -2);
	if(!res.result || res.node == null){
		return res;
	} else{
		res.result = (head.data == res.node.data); 
		res.node = res.node.next;
		return res;
	}
}
boolean isPalindrome(Node head){
	Result p =isPal(head, listSize(head));
	return p.result;
}*/
}
